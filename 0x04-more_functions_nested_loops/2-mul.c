#include <stdio.h>
#include "main.h"

/**
 * mul - prints the product of the two integers
 * @a: number to be inputed
 * @b: number to be inputed
 * Return: multiply as the result
 */

int mul(int a, int b)
{
int multiply;

multiply = a * b;
return (multiply);
}
